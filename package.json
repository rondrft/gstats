{
  "name": "gstats",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub stats cards for your README — contributions, streaks and languages, rendered as SVG on the edge.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "dev": "wrangler dev --var SERVICE_VERSION:dev-$(git rev-parse --short HEAD)",
    "deploy": "pnpm deploy:primary && pnpm deploy:legacy",
    "deploy:primary": "wrangler deploy --env=\"\" --var SERVICE_VERSION:$(git rev-parse --short HEAD)",
    "deploy:legacy": "wrangler deploy --env legacy --var SERVICE_VERSION:$(git rev-parse --short HEAD)",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "vitest run",
    "samples": "vitest run test/samples.test.ts -u",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@cloudflare/workers-types": "5.20260728.1",
    "typescript": "^5.9.3",
    "vitest": "~4.1.10",
    "wrangler": "^4.115.0"
  }
}
